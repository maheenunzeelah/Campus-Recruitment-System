const mongoose = require('mongoose');
const Schema = mongoose.Schema;

const compSchema = new mongoose.Schema({

name: String,
email: String,
password: String,


});

var Company = mongoose.model("Company", compSchema);


module.exports=Company;